(
    (defun-inline construct_bid_truths (index amount)
        (c index amount)
    )

    (defun-inline bid_index (bid)
        (f bid)
    )

    (defun-inline bid_amount (bid)
        (r bid)
    )
)
